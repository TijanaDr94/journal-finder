# The service works without an API key (BM25 mode), but LLM reranking produces better results when the key is provided.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
SCORING_MODE=hybrid

# Comma-separated list of allowed CORS origins (default: *)
CORS_ORIGINS=*